@model WEB_KHACHSAN_MVC.Models.LOAIDICHVU

@{
    ViewBag.Title = "EditLoaiDichVu";
    Layout = "~/Views/Shared/_LayoutEmployee.cshtml";
}

<h2>EditLoaiDichVu</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>LOAIDICHVU</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.MALOAIDICHVU)

        <div class="form-group">
            Tên Loại Dịch Vụ
            <div class="col-md-10">
                @Html.EditorFor(model => model.TENLOAIDICHVU, new { htmlAttributes = new { @class = "form-control", required = "required" } })
                @Html.ValidationMessageFor(model => model.TENLOAIDICHVU, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "ListLoaiDichVu")
</div>

